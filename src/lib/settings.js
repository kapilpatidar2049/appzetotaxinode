// Settings / configuration helpers (DB-backed).

const {
  getSettings,
  getMapSettings,
  getPaymentSettings,
  getFirebaseSettings,
  getSmsSettings,
  getActiveSmsSettings,
  appName,
  activeLanguages,
  defaultLanguage,
} = require("../utils/phpHelpers");

module.exports = {
  getSettings,
  getMapSettings,
  getPaymentSettings,
  getFirebaseSettings,
  getSmsSettings,
  getActiveSmsSettings,
  appName,
  activeLanguages,
  defaultLanguage,
};

