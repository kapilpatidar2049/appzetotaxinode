/**
 * Spatial zone lookups from Laravel helpers — requires Zone / PeakZone / Airport
 * Mongoose models with GeoJSON polygons. Returns null until models exist.
 */

async function findPeakZone() {
  return null;
}

async function findZone() {
  return null;
}

async function findAirport() {
  return null;
}

module.exports = {
  findPeakZone,
  findZone,
  findAirport,
};
