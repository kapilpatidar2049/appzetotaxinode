/**
 * Helpers/Referral/ReferralHelper.php — large trait (commission, rewards).
 * Port business rules into a dedicated service; this module is a stub barrel.
 */
module.exports = {
  /** TODO: port from ReferralHelper.php when referral flow is wired in Node */
  placeholder: true,
};
